{
  "fields": {
    "project": {
      "id": "${projectId}"
    },
    "summary": "${summary}",
    "issuetype": {
      "id": "${issueType}"
    },
    "assignee": {
      "name": "${assignee}"
    },
    "reporter": {
      "name": "${reporter}"
    }
  }
}